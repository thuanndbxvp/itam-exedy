fix(ui): Sprint C.3 — Flatten sidebar nav to 5 groups + horizontal account tabs

Part 1: Sidebar navigation refactored from nested dropdown (SETTINGS_GROUPS)
into 5 flat groups with group headers (Tong quan, Quan ly Tai san, Van hanh
& Ho tro, Nhan su & To chuc, He thong). Removed RoleGate wrapper (items
filtered by allowedRoles+permissionKey inline). Added group-level active
highlight. Added mobile close-on-nav.

Part 2: Account layout changed from flex-row (sidebar) to flex-col with
horizontal tab nav. UserPanelNav is now a border-bottom tab bar with
blue active indicator, matching GitHub Settings style. Initials/avatar
removed per MSEW spec.
